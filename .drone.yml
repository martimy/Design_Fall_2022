kind: pipeline
type: docker
name: Testing Python CI/CD


steps:
- name: Install and deploy
  image: martimy/netauto:0.1
  commands:
  - python bat.py

trigger:
  branch:
    exclude:
    - main
    - master

